# 종합 커리어 분석 모듈 실행 의존성
# career_analysis_comprehensive.py / hallucination_guard.py — 2개 파일 실행에 필요

google-genai>=1.0   # Gemini 클라이언트. 이 세션에서 검증한 버전: 2.14.0
pypdf>=4.0           # PDF 입력 파싱
pydantic>=2.0        # 응답 모델(VectorSuccessResponse/ErrorResponse). 필수 —
                     # 경량 대체 구현은 규칙 위반이라 제거함(2026-07-29).
                     # 이 세션에서 검증한 버전: 2.13.4
